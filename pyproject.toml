[project]
name = "shirin"
version = "2.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "matplotlib",
    "seaborn",
    "numpy",
    "pandas",
]

[dependency-groups]
dev = [
    "jupyter",
    "ipykernel",
    "pytest>=8.4.2",
]
